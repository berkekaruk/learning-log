#include <stdio.h>  // Import standard IO library for printf

int main(void) {
  printf("Hello, World\n");  // Print formatted output to console

  return 0;  // Return success status to the operating system (Best practice)
}